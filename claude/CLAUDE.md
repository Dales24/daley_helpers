# CLAUDE.md — project instructions template

Drop this file at a repo root so Claude Code picks up project conventions on every run.
Delete the sections you don't need.

## Stack
- Language / framework: <fill in>
- Package manager / build: <fill in>
- Test runner: <fill in>

## Conventions
- Keep changes minimal and focused on what was asked.
- Match the surrounding code's style; don't reformat unrelated lines.
- Write a test for every behavioral change.

## Commands
- Install: `<cmd>`
- Test: `<cmd>`
- Lint/format: `<cmd>`
- Run: `<cmd>`

## Don't
- Don't commit secrets or `.env` files.
- Don't push directly to the default branch — open a PR.
